// The (dashboard) route group's page.tsx already serves the root path.
// This file is intentionally left empty — Next.js uses (dashboard)/page.tsx
// for the '/' route since that group has no URL segment.
export { default } from './(dashboard)/page'
